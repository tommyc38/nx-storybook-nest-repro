import { CorMeasureElementDirective } from './measure-element.directive';

describe('MeasureElementDirective', () => {
  it('should create an instance', () => {
    const directive = new CorMeasureElementDirective();
    expect(directive).toBeTruthy();
  });
});
